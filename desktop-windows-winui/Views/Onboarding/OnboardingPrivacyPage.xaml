<Page
    x:Class="TranscribeDesktop.WinUI.Views.Onboarding.OnboardingPrivacyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource AppBackgroundBrush}">

    <Grid Padding="28">
        <Border Padding="24" Background="{StaticResource AppPanelBrush}" CornerRadius="12">
            <StackPanel Spacing="14">
                <TextBlock Text="Data sharing" FontSize="30" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" />
                <TextBlock Text="All transcription stays local. You can optionally allow anonymous diagnostics (startup failures, installer failures) to improve stability." TextWrapping="Wrap" Foreground="#FF94A3B8" />

                <ToggleSwitch x:Name="DiagnosticsToggle" Header="Allow anonymous diagnostics" />
                <TextBlock Text="No transcript or media content is sent by this setting." Foreground="#FFE2E8F0" />

                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,10,0,0">
                    <Button Content="Back" Click="BackButton_Click" />
                    <Button Content="Continue" Click="NextButton_Click" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
