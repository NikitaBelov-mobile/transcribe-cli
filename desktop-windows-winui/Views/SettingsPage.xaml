<Page
    x:Class="TranscribeDesktop.WinUI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource AppBackgroundBrush}">

    <Grid Padding="20">
        <Border Padding="20" Background="{StaticResource AppPanelBrush}" CornerRadius="12">
            <StackPanel Spacing="14">
                <TextBlock Text="Settings" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" />

                <ToggleSwitch x:Name="DiagnosticsToggle" Header="Allow anonymous diagnostics" />
                <TextBlock x:Name="PreferredModelText" Foreground="#FFE2E8F0" />
                <TextBlock x:Name="StatePathText" Foreground="#FF94A3B8" TextWrapping="Wrap" />

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Save" Click="SaveButton_Click" />
                    <Button Content="Restart onboarding" Click="RestartOnboardingButton_Click" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
